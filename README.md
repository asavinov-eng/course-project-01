# Your Energy - Exercise Application

Сучасний веб-застосунок для перегляду та пошуку вправ для тренування. Досліджуйте тисячі вправ з детальною інформацією, рейтингами та персоналізованими рекомендаціями.

## 🎯 Можливості

- **Цитата дня** - Щодня нова мотивуюча цитата
- **Каталог вправ** - Перегляд тисяч вправ з фільтрами
- **Розширена фільтрація** - Фільтрація за м'язами, частинами тіла або обладнанням
- **Пошук** - Пошук вправ за назвою або ключовим словом
- **Деталі вправи** - Перегляд повної інформації про вправу в модальному вікні
- **Система рейтингів** - Оцінювання вправ та перегляд оцінок спільноти
- **Пагінація** - Зручна навігація списками вправ
- **Підписка на новини** - Підписка на отримання нових оновлень
- **Адаптивний дизайн** - Ідеально працює на мобільних, планшетах та комп'ютерах
- **Оптимізація продуктивності** - Швидке завантаження та плавні взаємодії

## 🚀 Швидкий старт

### Вимоги

- Node.js (v14 або вище)
- npm або yarn

### Встановлення

1. Скопіюй репозиторій:
```bash
git clone https://github.com/yourusername/your-energy.git
cd your-energy
```

2. Встанови залежності:
```bash
npm install
```

3. Запусти сервер розробки:
```bash
npm run dev
```

Застосунок буде доступний за адресою `http://localhost:5173`.

### Збірка для продакшену

```bash
npm run build
```

Це створить оптимізоване складання в папці `dist/`.

### Перегляд збірки продакшену

```bash
npm run preview
```

## 📦 Структура проекту

```
src/
├── js/
│   ├── api-service.js        # Модуль API комунікації
│   ├── app-controller.js     # Основна логіка застосунку
│   ├── constants.js          # Константи та конфігурація
│   └── ui-helpers.js         # Утилітні функції UI
├── css/
│   ├── styles.css            # Головний CSS файл
│   ├── base.css              # Базові стилі та змінні
│   ├── responsive.css        # Медіа запити
│   └── [інші CSS модулі]
├── partials/
│   ├── header.html           # Заголовок
│   ├── footer.html           # Підвал
│   ├── quote.html            # Секція цитат
│   ├── filters.html          # Вкладки фільтрів
│   ├── search.html           # Пошуковий вхід
│   ├── subscription.html     # Форма підписки
│   └── modal-exercise.html   # Модаль деталей вправи
├── index.html                # Головна сторінка
├── main.js                   # Точка входу
└── public/
```

## 🔗 API

Застосунок використовує Your Energy API:

- **GET /api/quote** - Отримати цитату дня
- **GET /api/filters** - Отримати доступні фільтри
- **GET /api/exercises** - Отримати вправи з опціональними фільтрами
- **GET /api/exercises/{id}** - Отримати деталі вправи
- **PATCH /api/exercises/{id}/rating** - Оцінити вправу
- **POST /api/subscription** - Підписатися на новини

API документація: https://your-energy.b.goit.study/api-docs/

## 🎨 Дизайн

Дизайн застосунку базується на макеті Figma:
https://www.figma.com/file/1ifqGcQBIzMoc21yIqyV5q/Your-energy

## 📱 Адаптивний дизайн

- Мобільні пристрої (360px і більше)
- Планшети (768px і більше)
- Комп'ютери (1024px і більше)
- Великі екрани (1440px і більше)

## ✅ Якість коду

- HTML валідація (W3C)
- CSS валідація (W3C)
- Продуктивність 90%+ (Google PageSpeed)
- Доступність (ARIA, semantic HTML)

## 🌐 Деплой

Автоматичний деплой на GitHub Pages через GitHub Actions.

## 🛠️ Технології

- **Vanilla JavaScript (ES6+)** - Без фреймворків
- **HTML5** - Семантична розмітка
- **CSS3** - Сучасні стилі зі змінними CSS
- **Vite** - Інструмент збірки
- **Modern Normalize** - CSS нормалізація

## 📝 Стиль коду

- **Змінні**: camelCase
- **Класи**: PascalCase
- **Константи**: UPPER_SNAKE_CASE
- **Імена файлів**: lowercase-with-hyphens

## 📄 Ліцензія

ISC

---

**Розроблено з ❤️ на курсі GoIT Academy**

  `header.html` створюємо у папці `partials` та імпортуємо в `index.html`.
- Файли стилів повинні лежати в папці `src/css` та імпортуватись до HTML-файлів
  сторінок. Наприклад, для `index.html` файл стилів називається `index.css`.
- Зображення додавай до папки `src/img`. Збирач оптимізує їх, але тільки при
  деплої продакшн версії проекту. Все це відбувається у хмарі, щоб не
  навантажувати твій комп'ютер, тому що на слабких компʼютерах це може зайняти
  багато часу.

## Деплой

Продакшн версія проекту буде автоматично збиратися та деплоїтись на GitHub
Pages, у гілку `gh-pages`, щоразу, коли оновлюється гілка `main`. Наприклад,
після прямого пуша або прийнятого пул-реквесту. Для цього необхідно у файлі
`package.json` змінити значення прапора `--base=/<REPO>/`, для команди `build`,
замінивши `<REPO>` на назву свого репозиторію, та відправити зміни на GitHub.

```json
"build": "vite build --base=/<REPO>/",
```

Далі необхідно зайти в налаштування GitHub-репозиторію (`Settings` > `Pages`) та
виставити роздачу продакшн версії файлів з папки `/root` гілки `gh-pages`, якщо
це не було зроблено автоматично.

![GitHub Pages settings](./assets/repo-settings.png)

### Статус деплою

Статус деплою крайнього коміту відображається іконкою біля його ідентифікатора.

- **Жовтий колір** - виконується збірка та деплой проекту.
- **Зелений колір** - деплой завершився успішно.
- **Червоний колір** - під час лінтингу, збірки чи деплою сталася помилка.

Більш детальну інформацію про статус можна переглянути натиснувши на іконку, і в
вікні, що випадає, перейти за посиланням `Details`.

![Deployment status](./assets/deploy-status.png)

### Жива сторінка

Через якийсь час, зазвичай кілька хвилин, живу сторінку можна буде подивитися за
адресою, вказаною на вкладці `Settings` > `Pages` в налаштуваннях репозиторію.
Наприклад, ось посилання на живу версію для цього репозиторію

[https://goitacademy.github.io/vanilla-app-template/](https://goitacademy.github.io/vanilla-app-template/).

Якщо відкриється порожня сторінка, переконайся, що у вкладці `Console` немає
помилок пов'язаних з неправильними шляхами до CSS та JS файлів проекту
(**404**). Швидше за все у тебе неправильне значення прапора `--base` для
команди `build` у файлі `package.json`.

## Як це працює

![How it works](./assets/how-it-works.png)

1. Після кожного пуша у гілку `main` GitHub-репозиторію, запускається
   спеціальний скрипт (GitHub Action) із файлу `.github/workflows/deploy.yml`.
2. Усі файли репозиторію копіюються на сервер, де проект ініціалізується та
   проходить лінтинг та збірку перед деплоєм.
3. Якщо всі кроки пройшли успішно, зібрана продакшн версія файлів проекту
   відправляється у гілку `gh-pages`. В іншому випадку, у лозі виконання скрипта
   буде вказано в чому проблема.
